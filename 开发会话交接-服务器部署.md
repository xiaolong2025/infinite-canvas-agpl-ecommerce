# 无限画布开发会话交接说明

> 2026-07-21 登录与服务端托管 API 的最新部署要求、运行时环境、健康检查和首次上线清单，优先读取 `部署会话交接-登录与托管API-2026-07-21.md`。其中与旧版“浏览器保存 API Key”设计冲突的内容，以新交接为准。

## 2026-07-21 认证与服务器托管 API 开发状态

当前源码已进入“登录与托管 API 第一阶段”，这一节优先于后文仍描述浏览器直存 API Key 的旧方案：

- 前端仍为 Vite + React，画布、素材和生成记录仍保存在浏览器本地，未迁移现有画布数据。
- Docker 运行镜像改为 Bun 单容器：同一个 `3000` 端口同时提供静态前端、登录会话、SQLite 管理接口和 `/api/ai/*` 受控代理。
- 管理员通过 `/config` 配置渠道、模型、启用状态和 API Key；API Key 使用 `APP_ENCRYPTION_KEY` 加密写入 SQLite，不返回浏览器。
- 普通用户只能读取管理员开放的模型名称和同源代理地址；当前登录会话使用 HttpOnly Cookie，用户可设置每日调用额度。
- 服务器必须额外提供不入 Git 的 `runtime.env` 和持久化数据目录。`/healthz` 在缺少加密密钥或初始管理员时返回失败，自动部署不会切换未初始化版本。
- 本阶段已完成源码、类型检查、生产前端构建和 Bun API 单元测试；Docker Desktop 不可用，真实镜像、容器、服务器、GHCR 和公网仍未验收。
- 未经用户再次确认，不得提交、推送、写入服务器密钥、修改服务器/Nginx/DNS、触发公网部署或调用真实付费模型。

## 当前实际部署状态（2026-07-20）

- 已完成用户自有仓库 `xiaolong2025/infinite-canvas-agpl-ecommerce` 的 `develop` 发布链路。
- 当前公网地址：`https://canvas.yigeai.work/`。
- 当前线上镜像、完整 SHA 和活动端口以服务器 `/opt/infinite-canvas/current-version` 与公网 `/build-info.json` 为准；部署在 `127.0.0.1:3100`、`127.0.0.1:3101` 之间双槽切换。
- 上一版本容器和镜像始终保留在另一候选端口，供回滚使用。
- 首次部署版本已通过健康、哈希 JS/CSS、`/build-info.json`、TLS、公网浏览器和基础画布交互验收；后续自动更新已实际验证。
- 已真实执行 `/opt/infinite-canvas/deploy.sh rollback` 并恢复最终版本，回滚证据有效。
- `infinite-canvas-deploy.timer` 已启用，每两分钟检查用户 GHCR 的 `dev` 标签并按 OCI 完整 SHA 安全切换。
- 部署前后 `api.yigeai.work/health` 均为 HTTP 200；现有 YigeAI Nginx 配置哈希、Uptime Kuma 容器 ID/启动时间/healthy 状态均未变化。
- 入口机未修改或重启 Sub2API、PostgreSQL、Redis、YigeAI API 或现有监控。

## 一、这次任务到底要解决什么

当前项目目录：

```text
E:\项目\04_需求池\事项项目\01_在办\无限画布
```

这是一个已经存在的无限画布项目源码。用户正在这个项目上进行二次开发，不需要重新开发、重新复刻或重新搭建一套无限画布。

用户的真实目标是：

```text
继续在当前源码上开发
  -> 用户自己的开发版本能够构建
  -> 用户访问一个固定网站地址
  -> 网站运行的是用户当前开发版本
  -> 后续代码发布后，服务器可以自动更新
  -> 初期只给 1 至 2 名用户使用
  -> 更新无限画布时不影响现有 YigeAI API 中转服务
  -> 新版本失败时可以回滚到上一版本
```

## 二、必须理解的术语

### 1. “开发版本”是什么意思

开发版本不是作者 GitHub 上的版本，也不是作者的：

```text
ghcr.io/basketikun/infinite-canvas:latest
```

开发版本是：

```text
当前目录中的源码
+ 用户已经完成的二次开发修改
+ 用户后续提交到自己仓库的修改
```

服务器必须运行由这份源码构建出来的镜像。

### 2. “实时更新”是什么意思

本项目的“实时更新”定义为：

```text
开发者修改代码
  -> 提交并推送指定部署分支
  -> 自动构建
  -> 自动部署
  -> 自动健康检查
  -> 自动切换新版本
  -> 用户刷新网页后使用新版本
```

它不等于：

- 本地每保存一次文件，服务器就立即同步。
- 把 Vite 开发服务器直接暴露到公网。
- 用户正在打开页面时自动替换浏览器内已经加载的 JavaScript。
- 不经过构建、检查和回滚保护就直接覆盖线上文件。

本地开发使用 Vite HMR；公网使用构建后的 Nginx 静态文件。用户通常需要刷新页面才能加载新版本。

### 3. “部署成功”是什么意思

以下情况单独出现时，都不能算部署成功：

- 本地代码已经写完。
- Docker 镜像构建成功。
- Docker 容器启动成功。
- 服务器某个端口返回 `200`。
- GitHub Actions 显示完成。

必须同时证明：

1. 服务器运行的是用户自己的镜像。
2. 镜像对应当前 Git SHA。
3. 公网域名加载的是当前二次开发版本。
4. 页面可以完成基础画布操作。
5. 页面可以使用用户配置的 YigeAI Base URL 发起 API 请求。
6. 现有 `api.yigeai.work` 没有被重启或中断。
7. 新版本失败时旧版本可以恢复。

## 三、当前项目真实结构

- 前端目录：`web`
- 技术栈：Vite、React、TypeScript
- 本地开发命令：`cd web && bun run dev`
- 构建命令：`cd web && bun run build`
- 类型检查：`cd web && bun run typecheck`
- Docker 构建阶段：Bun
- Docker 运行阶段：Nginx
- Docker 容器内部端口：`3000`
- 主应用是静态前端，不是带数据库的后端 SaaS。
- 当前项目没有要求为主应用额外部署 PostgreSQL 或 Redis。
- 画布、素材、生成记录和 API Key 默认保存在用户浏览器本地。
- AI API 请求由浏览器前台直连用户配置的 OpenAI 兼容接口。

当前仓库的 `docker-compose.yml` 使用作者的远程镜像：

```yaml
services:
  app:
    image: ghcr.io/basketikun/infinite-canvas:latest
```

这只能用于体验作者发布版，不能用于用户自己的开发版部署。

## 四、当前工作区的处理规则

开发会话开始时必须先执行：

```bash
git status --short --branch
git remote -v
```

当前目录已经存在用户的二次开发修改。开发会话必须：

- 先盘点修改；
- 不回滚用户修改；
- 不执行 `git reset --hard`；
- 不执行 `git checkout --`；
- 不删除未跟踪文件；
- 不把用户修改覆盖成作者远程版本；
- 不擅自把所有修改提交或推送；
- 不把当前本地修改误认为已经部署。

首次发布前，必须由用户确认哪些修改属于第一个可部署版本。

## 五、推荐的最终部署结构

### 服务器位置

优先部署在 NovixLink 入口机：

```text
NovixLink 入口机
  -> Nginx
  -> canvas.yigeai.work
  -> 无限画布容器 127.0.0.1:3100 或 127.0.0.1:3101
```

RackNerd 源站继续只承担现有 YigeAI API 中转、数据库、Redis 和监控，不作为无限画布的构建机。

如果入口机不适合运行 Docker，则使用备用方案：

```text
本地构建 web/dist
  -> 上传到入口机版本目录
  -> Nginx 直接提供静态文件
```

不能因为入口机不适合 Docker，就把无限画布构建任务放到正在承载 API 的 RackNerd 源站。

### 网站地址

建议使用独立域名：

```text
canvas.yigeai.work
```

这个域名只是方案建议。真正修改 DNS、申请证书和新增公网站点前，必须先向用户说明影响并获得确认。

不能把无限画布直接挂到：

```text
api.yigeai.work
```

也不能把无限画布的 Nginx 配置混入 API 的代理规则。

## 六、代码、镜像和服务器之间的关系

```text
用户本地源码
  -> 用户自己的 Git 仓库
  -> GitHub Actions 构建
  -> 用户自己的 GHCR 镜像
  -> 服务器拉取指定镜像
  -> 候选容器启动
  -> 健康检查
  -> Nginx 切换
  -> 用户访问网站
```

推荐镜像名：

```text
ghcr.io/<用户自己的账号>/infinite-canvas:<完整 Git SHA>
```

可以另外维护：

```text
ghcr.io/<用户自己的账号>/infinite-canvas:dev
```

但是服务器部署记录不能只记录 `dev`，必须同时记录完整 Git SHA。

不允许服务器使用作者的：

```text
ghcr.io/basketikun/infinite-canvas:latest
```

## 七、自动发布流程

### 阶段 A：本地开发

```text
修改代码
  -> 本地运行 Vite
  -> 浏览器测试
  -> 选择本次要发布的修改
  -> 提交 Git
  -> 推送 develop 分支
```

建议分支：

- `main`：已验收版本。
- `develop`：给 1 至 2 名内测用户使用的开发版本。

不是每次保存都发布。只有提交并推送到 `develop` 后才触发服务器更新。

### 阶段 B：GitHub Actions

`develop` 分支推送后，CI 必须：

1. 检出当前提交。
2. 安装 Bun 依赖。
3. 执行 `bun run typecheck`。
4. 执行 `bun run build`。
5. 构建 Linux amd64 镜像。
6. 使用当前 Git SHA 标记镜像。
7. 推送到用户自己的镜像仓库。
8. 只有构建和检查通过后，才调用服务器部署脚本。
9. 将 Git SHA、镜像地址、部署时间和部署结果写入 Actions 日志。

如果类型检查或构建失败：

- 不得连接服务器；
- 不得切换线上容器；
- 不得修改 Nginx；
- 必须保留当前线上版本。

### 阶段 C：服务器候选部署

服务器固定保留两个候选端口：

```text
127.0.0.1:3100
127.0.0.1:3101
```

当前线上版本使用其中一个端口，新版本先启动在另一个端口。

部署脚本必须：

1. 接收完整镜像标签。
2. 拉取指定镜像。
3. 判断当前线上端口。
4. 选择另一个端口启动候选容器。
5. 不停止当前线上容器。
6. 检查候选容器首页。
7. 检查候选容器静态资源是否能加载。
8. 检查候选版本号或 Git SHA。
9. 健康检查通过后才修改 Nginx 上游。
10. 执行 `nginx -t`。
11. 执行 Nginx reload。
12. 检查公网域名。
13. 记录当前线上镜像和 Git SHA。
14. 保留上一版本容器和镜像。

健康检查至少包括：

```bash
curl --fail --silent --show-error http://127.0.0.1:<候选端口>/
```

公网检查至少包括：

```bash
curl --fail --silent --show-error https://canvas.yigeai.work/
```

## 八、回滚规则

候选版本出现以下任一情况，不能切换线上：

- 容器启动失败。
- 首页不是 `200`。
- 静态 JS 或 CSS 加载失败。
- 版本标识不匹配。
- Nginx 配置检查失败。
- 公网域名切换后不是 `200`。
- 页面初始化报错。

已经切换后发现异常时：

1. Nginx 切回上一端口。
2. 执行 Nginx reload。
3. 检查公网域名。
4. 保留失败容器、镜像和日志。
5. 记录失败 Git SHA。
6. 不执行 `docker compose down`。
7. 不重启 YigeAI API。

回滚只恢复无限画布，不得影响：

- `api.yigeai.work`
- Sub2API
- PostgreSQL
- Redis
- YigeAI 现有监控

## 九、更新后用户如何看到新版本

正常情况下：

```text
开发者推送 develop
  -> CI 构建
  -> 服务器切换
  -> 用户刷新网页
  -> 用户加载新版本
```

需要配置：

- `index.html` 不长期缓存；
- `config.js` 不缓存；
- 带哈希的 JS、CSS、图片可以长期缓存；
- 页面显示当前版本号或 Git SHA。

不能通过删除用户浏览器缓存来解决每次部署问题。

## 十、用户数据和 API Key

当前项目数据主要在浏览器本地，因此：

- 同一个域名下刷新和发布，通常继续使用同一浏览器数据；
- 更换域名会导致浏览器看不到原域名下的数据；
- 清理浏览器站点数据会丢失本地画布和素材；
- 本地存储结构变化可能导致旧数据不能直接读取；
- 发布涉及画布数据结构的修改前，必须先导出或备份测试用户数据。

API Key 规则：

- API Key 由用户在浏览器中填写；
- 不得写进前端源码；
- 不得写进 Dockerfile；
- 不得写进镜像环境变量；
- 不得写进 GitHub Actions；
- 1 至 2 名用户使用独立、限额的 Key；
- 不使用一个公开共享的高权限 Key。

如果后续要改成服务器统一代理 API，必须另立项目阶段，重新设计认证、限流、费用和密钥保护，不能在本方案里顺手加入。

## 十一、正式部署前的只读预检

在任何生产修改之前，先只读检查目标入口机：

```bash
hostname
free -h
df -h /
uptime
docker version
docker ps
ss -lntp
nginx -t
```

还要确认：

- 入口机当前可以登录；
- Docker 服务正常；
- Nginx 正常；
- `3100` 和 `3101` 未被占用；
- 磁盘可以保存当前版和上一版镜像；
- 入口机的 Nginx 配置位置；
- DNS 是否已经指向入口机；
- HTTPS 证书是否可以为新域名签发；
- `api.yigeai.work` 是否允许 `canvas.yigeai.work` 的浏览器请求；
- 现有 API 入口不需要修改或重启。

如果预检失败，状态是“阻塞”，不能假装已经可以部署。

## 十二、必须由用户确认的事项

以下动作不能由开发会话默认执行：

- 创建或修改 GitHub 仓库；
- 推送当前本地代码；
- 创建 GHCR 镜像；
- 配置 GitHub Actions Secrets；
- 保存服务器 SSH 私钥；
- 修改 DNS；
- 修改 Nginx；
- 申请或续期公网证书；
- 修改服务器 Docker Compose；
- 启动公网容器；
- 进行真实付费生图；
- 修改 YigeAI API 的 CORS、Key、分组或计费配置。

开发会话可以先完成：

- 代码盘点；
- 本地部署配置草案；
- CI 文件草案；
- 服务器部署脚本草案；
- Nginx 配置草案；
- 本地构建；
- 本地页面验收；
- 只读服务器预检。

## 十三、开发会话必须交付的文件

最终至少应有：

```text
docker-compose.server.yml
.github/workflows/deploy-dev.yml
ops/deploy-infinite-canvas.sh
ops/nginx/canvas.yigeai.work.conf
docs/content/docs/overview/yigeai-development-deployment-plan.mdx
```

如果实际采用直接上传 `web/dist` 的备用方案，则还需要：

```text
ops/deploy-static-dist.ps1
ops/deploy-static-dist.sh
```

所有脚本必须：

- 不包含密码、Token、API Key 或私钥；
- 接收环境变量或参数；
- 输出当前 Git SHA 和镜像标签；
- 失败时返回非零退出码；
- 不删除当前线上版本；
- 支持回滚。

## 十四、完整验收清单

第一次正式部署前后，逐项提供证据：

### 代码和镜像

- [ ] 当前发布 Git SHA。
- [ ] 用户自己的镜像地址。
- [ ] 镜像构建成功。
- [ ] 镜像不是作者的 `latest`。
- [ ] CI 类型检查通过。
- [ ] CI 构建通过。

### 服务器

- [ ] 入口机预检通过。
- [ ] 候选端口可用。
- [ ] 候选容器健康检查通过。
- [ ] Nginx 配置检查通过。
- [ ] Nginx reload 成功。
- [ ] 上一版本仍保留。

### 公网网站

- [ ] `https://canvas.yigeai.work/` 返回 `200`。
- [ ] 页面显示当前版本标识。
- [ ] 画布可打开。
- [ ] 拖拽、缩放、添加节点正常。
- [ ] 刷新后画布数据仍可读取。
- [ ] 页面可以调用用户配置的 YigeAI Base URL。

### YigeAI API 保护

- [ ] `api.yigeai.work` 没有重启。
- [ ] 现有 API 健康检查仍正常。
- [ ] 现有 API Key、账号、分组和计费没有改变。
- [ ] 没有默认执行付费生图。

### 回滚

- [ ] 可以列出当前线上镜像。
- [ ] 可以列出上一版本镜像。
- [ ] 可以切回上一版本。
- [ ] 切回后公网网站恢复。
- [ ] 回滚没有影响 YigeAI API。

## 十五、给开发会话的直接指令

把本文件和项目根目录的 `AGENTS.md` 一起读取，然后执行以下任务：

```text
你正在维护的是一个已经存在的无限画布项目，不是重新开发一个新项目，也不是复刻 YigeAI。

当前源码目录：
E:\项目\04_需求池\事项项目\01_在办\无限画布

用户正在这个源码上进行二次开发。用户需要的是：
1. 1 至 2 名用户访问用户自己的开发版本；
2. 用户推送指定分支后，服务器自动更新；
3. 更新无限画布时不影响现有 YigeAI API；
4. 新版本失败时可以回滚。

请先读取：
E:\项目\04_需求池\事项项目\01_在办\无限画布\AGENTS.md
E:\项目\04_需求池\事项项目\01_在办\无限画布\开发会话交接-服务器部署.md
E:\项目\04_需求池\事项项目\01_在办\无限画布\docs\content\docs\overview\yigeai-development-deployment-plan.mdx

严格遵守：
- 不使用作者 ghcr.io/basketikun/infinite-canvas:latest 作为用户线上版本；
- 不重写无限画布；
- 不覆盖或回滚当前工作区已有修改；
- 不把 Vite dev server 暴露到公网；
- 不把构建任务放到 RackNerd API 源站；
- 不修改 YigeAI API、Sub2API、PostgreSQL、Redis；
- 不提交、推送、改 DNS、改 Nginx、配 SSH Secret 或触发公网部署，除非先列出影响并得到用户确认；
- 不展示或写入任何密码、Token、API Key、Cookie 或私钥；
- 不执行真实付费生图作为默认验收。

第一阶段只做：
1. 读取并盘点当前工作区和未提交修改；
2. 设计用户自己的镜像地址；
3. 创建服务器 Compose 配置草案；
4. 创建 GitHub Actions 自动构建和部署草案；
5. 创建候选容器健康检查、Nginx 切换和回滚脚本草案；
6. 先完成本地构建和页面验收；
7. 列出服务器只读预检结果；
8. 明确哪些变量、域名、密钥和服务器信息仍需用户提供或确认。

完成时必须说明：
- 用户的开发版本如何进入镜像；
- 服务器如何知道要更新到哪个 Git SHA；
- 用户推送代码后多久进入部署；
- 部署失败如何保留旧版本；
- 如何证明 YigeAI API 没有被重启；
- 如何证明公网网站加载的是用户当前版本；
- 当前哪些动作还没有执行。
```

## 十六、最终完成标准

只有满足以下条件，才可以说“开发版网站已经部署完成”：

```text
服务器运行用户自己的镜像
  + 镜像对应当前 Git SHA
  + 公网域名加载当前二次开发页面
  + 1 至 2 名用户可以正常使用
  + API 请求链路正常
  + YigeAI API 没有中断
  + 新版本失败可以回滚
```

仅有代码、镜像、容器或 CI 状态，不足以证明网站已经完成。
