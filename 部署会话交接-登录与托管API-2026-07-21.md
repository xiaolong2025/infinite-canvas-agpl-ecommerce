---
title: 无限画布登录与托管 API 部署交接
type: deployment-handoff
status: ready-for-deployment-session
updated: 2026-07-21
---

# 部署会话交接：登录与托管 API

## 结论

- **源码实现与自动化检查：PASS**
- **Docker 镜像实际运行：NEEDS WORK**
- **浏览器登录和管理台真实交互：NEEDS WORK**
- **GitHub Actions 与 GHCR：尚未真实触发**
- **服务器及公网更新：由部署会话处理**

本交接只证明当前二次开发源码已经完成登录与服务端托管 API 的实现，并通过现有本地自动化检查。不能据此直接宣称镜像、服务器或公网版本已经可用。

## 2026-07-21 部署后 413 修复

- 管理员实测“产品资料 → 生成 3 个钩子”时，浏览器收到 Nginx `413 Request Entity Too Large`。
- 开发会话接管 Chrome 后独立确认公网构建 SHA 仍为 `205ff9bdfff9c1559e3b16a135cc6edb7af7a411`，线上没有包含本节修复。
- 同一画布的“图片反推提示词”配置显示 1 张参考图，模型为 `gemini-2.5-pro（YigeAI Gemini 拆解）`；管理台模型行实际为 `gemini-2.5-pro | text | image-ref`。
- 使用当前 1254×1254 商品图执行一次反推测试，新增文本结果同样返回 Nginx 413 HTML；没有获得新的视觉反推结果。
- 已确认画布入口 Nginx 模板没有配置请求体上限，产品参考图以 base64 随 `/api/ai/` 请求提交时可能先被默认上限拒绝；仅凭截图不能完全排除 YigeAI 上游的第二层 Nginx。
- 源码修复：`ops/nginx/canvas.yigeai.work.conf` 对 `/api/ai/` 单独设置 `client_max_body_size 64m`。
- 前端修复：`web/src/services/ai-vision-input.ts` 会为文本模型生成不覆盖原资产的压缩副本，单图和多关键帧共享约 560KB 原始图片预算；HTTP 413 或 Nginx HTML 错误页转换为中文错误，不再写入工作流文本节点。
- 反推修复：新建反推配置标记为必须使用视觉输入；缺少参考图或模型未启用 `image-ref` 时直接阻止请求；反推指令禁止替换主体和沿用无关幻想示例。
- 本地验证：`npm run typecheck`、`npm run build`、`git diff --check` 通过。
- 部署会话必须先更新画布服务器上的 Nginx 模板和正式配置，执行 `nginx -t` 后 reload，再用同一产品图重新生成 3 个钩子。
- 部署后先访问 `/build-info.json`，确认公网 SHA 已不再是 `205ff9bd...`；再用同一商品图重试“反推提示词”，输出必须描述黑色街头 T 恤人物，不能再出现精灵森林等无关主体。
- 如果仍返回 413，先根据画布入口与 YigeAI 入口的 access/error log 只读确认是哪一层拒绝；未经用户单独确认，不修改 YigeAI Nginx 或 API 服务。

## 已完成的开发内容

1. 新增 Bun 服务端，单进程同时提供静态前端、认证、SQLite 和 AI 代理。
2. 新增管理员创建账号、编辑用户、停用账号、角色和每日调用额度。
3. 不开放公开注册。
4. 密码使用 Argon2id 哈希；登录使用 HttpOnly、SameSite=Lax 会话 Cookie。
5. 模型渠道 API Key 使用 AES-GCM 加密后写入 SQLite，管理接口只返回末四位预览。
6. 普通用户只获取管理员开放的模型，不获取真实上游 Base URL 或 API Key。
7. Gemini 原生和 OpenAI 兼容请求统一经过 `/api/ai/<channelId>/...`。
8. 代理限制 HTTP 方法、接口路径、模型白名单、同源请求和每日额度。
9. 底层 `/v1/models` 不向普通用户开放，模型列表以 `/api/models` 的管理员白名单为准。
10. 产品显示名称调整为“一格画布”，原画布、视频拆解、剧本和故事板工作流没有删除。

## 本地证据

- server TypeScript：通过。
- server Bun 测试：`3 pass / 13 expect`。
- web TypeScript：通过，使用 `tsc --noEmit --incremental false`。
- web production build：通过。
- Docker Compose 展开：通过。
- `ops/deploy-infinite-canvas.sh` 与 `ops/poll-and-deploy.sh` Bash 语法：通过。
- `git diff --check`：通过。
- 密钥扫描：只命中示例占位符和测试变量，没有发现真实 API Key、Token 或密码。

## 未完成证据

- 本机 Docker Desktop Linux daemon 未运行，未真实执行 `docker build`、容器启动、数据卷和容器内 `/healthz`。
- 临时本地后台进程启动被执行策略拦截，未完成浏览器端登录、管理台、退出和权限跳转验收。
- 未触发 GitHub Actions，未产生或拉取用户自己的 GHCR SHA 镜像。
- 未连接服务器，未修改服务器、Nginx、DNS、证书和现有容器。
- 未写入真实 Gemini、Kedaya、ZZ 或 ZZ-Gemini 密钥，未发起真实或付费模型调用。

## 重要运行契约

### 持久化

- SQLite 默认路径：`/app/data/canvas.sqlite`。
- 服务器宿主目录建议：`/opt/infinite-canvas/data`。
- 必须持久化挂载 `/app/data`，否则重新创建容器会丢失用户、会话、渠道密文和额度记录。
- 当前画布项目和“我的素材”仍主要保存在客户浏览器本地；增加登录不会自动把这些数据同步到服务器。

### 运行时环境

真实环境文件建议放在：

```text
/opt/infinite-canvas/runtime.env
```

必须配置的字段：

```dotenv
APP_ORIGIN=https://canvas.yigeai.work
APP_SECURE_COOKIES=true
APP_ENCRYPTION_KEY=<32 字节随机值的 Base64 编码>
BOOTSTRAP_ADMIN_USERNAME=<首次管理员用户名>
BOOTSTRAP_ADMIN_PASSWORD=<首次管理员强密码>
```

要求：

- 不要把真实 `runtime.env`、密钥或密码提交到 Git。
- `APP_ENCRYPTION_KEY` 必须固定并备份。渠道创建后更换或丢失该值，会导致既有 API Key 无法解密，`/healthz` 返回 503。
- 首次管理员成功登录并确认 SQLite 数据卷跨容器保留后，从 `runtime.env` 移除 `BOOTSTRAP_ADMIN_PASSWORD`。
- 已有用户数据库存在时，不需要继续保留首次管理员密码。

### 健康检查

`/healthz` 只有同时满足以下条件才返回 200：

1. SQLite 中至少存在一个用户。
2. `APP_ENCRYPTION_KEY` 格式正确。
3. 所有已保存渠道密钥均可被当前加密密钥解密。

因此首次候选容器必须带首次管理员环境变量启动。后续候选容器必须挂载同一数据目录并使用同一 `APP_ENCRYPTION_KEY`。

## 推送影响

当前分支为 `develop`，`origin` 是用户自己的仓库：

```text
https://github.com/xiaolong2025/infinite-canvas-agpl-ecommerce.git
```

向 `develop` 推送会触发 `.github/workflows/deploy-dev.yml`：

1. 安装并检查前后端依赖。
2. 执行 web typecheck、server typecheck、server test 和 web build。
3. 构建并推送：

```text
ghcr.io/xiaolong2025/infinite-canvas:<完整 Git SHA>
ghcr.io/xiaolong2025/infinite-canvas:dev
```

如果服务器的轮询 Timer 已经启用，`dev` 更新可能自动触发候选部署并改变 `canvas.yigeai.work`。因此提交、推送和服务器准备必须由部署会话先说明影响并取得用户确认。

## 部署会话执行顺序

1. 完整读取根目录 `AGENTS.md`、`开发会话交接-服务器部署.md` 和本文件。
2. 只读检查 `git status --short --branch`、`git remote -v` 和当前修改范围，不覆盖现有二开。
3. 检查服务器 Timer 是否会在 `develop` 推送后自动部署。
4. 在任何推送前，先准备并核对 `runtime.env` 字段状态和 `/opt/infinite-canvas/data` 权限，但真实写入仍需用户确认。
5. 用户确认后提交并推送 `develop`，观察 GitHub Actions 的每个步骤和 GHCR 完整 SHA 镜像。
6. 对入口机执行只读 Docker、磁盘、端口、Nginx、证书、当前容器和 YigeAI 健康预检。
7. 使用完整 SHA 镜像启动非活动端口候选容器，不使用作者镜像或 `latest`。
8. 验证候选 `/healthz`、首页静态资源和 `/build-info.json`。
9. 浏览器验证管理员登录、管理页、创建普通用户、普通用户无管理入口、退出和重新登录。
10. 管理员手动录入真实渠道密钥；不要通过聊天、日志或 Git 传递密钥。
11. 只有取得真实调用授权后，分别执行一次最小 Gemini 文本和生图渠道验证。
12. 图片反推复测时，文本模型和所选生图模型都必须启用 `image-ref`；确认八维拆解准确、生成配置保留原图参考，且结果主体、服装、构图和风格与原图一致。
13. 候选全部通过后再切换 Nginx；公网失败立即恢复上一端口。
14. 确认 `api.yigeai.work`、YigeAI 容器及相关数据库没有被重启或修改。

## 首次上线验收清单

- [ ] GitHub Actions 全部通过。
- [ ] GHCR 存在当前完整 Git SHA 镜像，镜像不是作者镜像且不是 `latest`。
- [ ] 候选容器挂载 `/opt/infinite-canvas/data:/app/data`。
- [ ] 候选 `/healthz` 返回 200。
- [ ] 候选 `/build-info.json` 返回当前 Git SHA。
- [ ] 管理员可以登录并访问“管理”。
- [ ] 普通用户不能访问管理页，也不能读取真实 API Key。
- [ ] 编辑渠道时留空 API Key 不会覆盖原密钥。
- [ ] 同一商品图的反推结果包含八维拆解和完整中文提示词，后续生图配置保留原图参考；无 `image-ref` 时明确阻止生成。
- [ ] 首次“生成 3 个剧情钩子”和“重新生成 3 案”都不再返回 413，且每次返回正好 3 个方案；如果参考图或 `image-ref` 缺失，前端应在请求前明确提示。
- [ ] 容器重建后用户和渠道仍存在。
- [ ] 使用错误 `APP_ENCRYPTION_KEY` 时候选健康检查失败，且不会切换 Nginx。
- [ ] 公网首页和 `/build-info.json` 指向当前 Git SHA。
- [ ] 回滚后上一容器恢复服务。
- [ ] 部署前后 YigeAI 健康检查正常，相关服务未被改动。

## 关键文件

- `server/src/api.ts`
- `server/src/database.ts`
- `server/src/security.ts`
- `server/tests/api.test.ts`
- `web/src/pages/login/index.tsx`
- `web/src/pages/config/index.tsx`
- `web/src/stores/use-auth-store.ts`
- `web/src/stores/use-config-store.ts`
- `Dockerfile`
- `docker-compose.server.yml`
- `.github/workflows/deploy-dev.yml`
- `ops/runtime.env.example`
- `ops/deploy-infinite-canvas.sh`
- `ops/poll-and-deploy.sh`
